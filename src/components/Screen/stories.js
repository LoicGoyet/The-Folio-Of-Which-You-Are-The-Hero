import React from 'react';
import { storiesOf } from '@storybook/react';

import Screen from '.';

const stories = storiesOf('Screen', module);

stories.add('default', () => <Screen />);

export default stories;
